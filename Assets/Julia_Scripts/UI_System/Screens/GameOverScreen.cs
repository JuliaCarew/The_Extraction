using UnityEngine;

public class GameOverScreen : UIScreen
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    public void RetryGame()
    {
        // logic from player / reset position & level
    }
}
